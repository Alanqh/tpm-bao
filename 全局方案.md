预留虚拟 TPM（Reserved vTPM）商品设计文档
0. 文档说明
本文档整理"预留虚拟 TPM"商品形态的设计讨论，覆盖产品定位、资源机制、SLO、定价、生命周期、库存运营、用户触点等关键模块。文末附关键 Q&A 汇总。
本文档处于需求澄清阶段，尚未开始实施，描述的是 vTPM 产品的目标形态。文中涉及的各项机制（转换系数、动态 RL、分级资源模型、SLO 赔付等）均为设计目标，不代表当前已具备能力。


1. 产品定位
1.1 一句话定位
预留 vTPM 是面向 LLM 推理的"算力资源切片租约"商品：用户按 30 天周期购买指定数量的虚拟 TPM（vTPM），获得对特定模型的最小资源预留与确定性 SLO 保障；平台通过 SLO 感知扩缩容 + Serverless 流量分级占用提升资源利用率。
1.2 在公有云产品矩阵中的定位
预留 vTPM 是 MaaS 层标准产品，与 Serverless API 同层。专属实例介于 MaaS 与部署层之间，FaaS 是底层部署形态，Tokens 包是支付手段，本文档中均仅作背景参照，不展开讨论。
产品路径
层级
本质
资源粒度
计费
SLO
Serverless API
MaaS 层
共享资源按量接入
无预留
后付费按 tokens
无 SLA
预留 vTPM
MaaS 层
算力切片租约
1/10 最小部署
预付资源费
分钟级 TPM + P95 延迟
专属实例（背景参照）
MaaS 与部署层之间
独占资源 + 定制 SLA
整最小部署
预付实例费
定制（TPM/TPS/总吞吐量）
FaaS 自部署（背景参照）
部署层
客户自部署云函数
副本粒度
按副本 + 用量
集群 SLA
Tokens 包（支付手段）
—
预付卡券抵扣
—
卡券抵扣
同 Serverless
与同层产品的边界：vs Serverless——Serverless 共享资源无保障，vTPM 是切片硬预留。
背景注脚：
- 专属实例与 vTPM 的核心区别：专属实例是独占资源 + 定制 SLA（客户可要求 TPM/TPS 或总吞吐量），vTPM 是标准化切片 + 标准化 SLO
- FaaS 是底层部署形态，可承载 MaaS 层推理服务，不限于语言模型；vTPM 和 Serverless 都是 MaaS 层标准产品
- 折算机制是平台级能力：多维系数归一化会同时作用于 vTPM 和未来的 vTokens 包（Tokens 包演进形态，按模型/输入/输出/长短等维度独立折算），但 vTokens 包与 vTPM 在相同或相似场景的系数不严格一致，各自独立定义
1.3 与 Serverless 的差异
维度
预留 vTPM
Serverless
本质
算力资源切片租约（硬预留）
按量计费的共享资源接入
计费
预付费（资源租约）
后付费（按 tokens）
SLO
分钟级 TPM 速率 + P95 延迟承诺
无 SLA，仅提供指标参考
容量保障
已售资源刚性预留，不可被侵占
可被 vTPM 回拉时分级抢占/中断
模型绑定
绑定具体 model name，可选专属 model name
默认接入模型最新版本
超限处理
严格限流，或降级到 Serverless 按量计费
直接限流
超售
vTPM 本身不超售
利用 vTPM 已售未用层借用资源
并发限制
严格限制。原因：vTPM 资源是硬预留的，单一客户超限会侵占刚性层，影响其他已购用户履约。限流基数 = 购买量按系数折算后的占用（固定，硬承诺）；低通滤波控制爬坡速率
动态 RL：限流基数由 PID 按 TPS 动态调整 + 客户宽容系数 + 低通滤波。现状为静态 Category 分级 Ratelimit，将被动态 RL 替代
1.4 适用场景
- 稳定的生产推理负载，需要可预期的吞吐与延迟
- 对成本可预期性要求高的企业客户
- 需要在 30 天窗口内锁定资源的业务（避免 Serverless 限流波动）
- 需要相对 Serverless 更高的瞬时爬坡容忍度（vTPM 低通滤波 15s × 2，远比 Serverless 90s × 1.2 宽松）
1.5 不适用场景
- 短时陡增的突发流量：vTPM 和 Serverless 均不接受，需在客户端做平滑/异步队列/预 Buffer
- 单次性推理任务（建议走 Serverless 按量计费）
- 超长周期（>30 天）稳定负载（vTPM 暂不提供长周期售卖）
- 需要独占资源或定制 SLA（可考虑专属实例，不在本文档范围）
- 需要自定义部署形态（可考虑 FaaS，不在本文档范围）
1.6 爬坡能力对照
低通滤波决定两类流量在突发场景下的实际响应能力（详见 §3.2.2）：
- vTPM 周期 15s × 2，15s 可翻倍并发，~50s 爬到 10 倍
- Serverless 周期 90s × 1.2，~342s 翻倍，~1134s 爬到 10 倍
关键含义：vTPM 用户拥有更高的瞬时爬坡容忍度，适合负载有自然波动的生产业务；Serverless 几乎不能接受陡增，主要承接稳态流量或客户端已做平滑的流量；真正的短时陡增场景两种产品都不适合，需在客户端做请求平滑、buffer、异步队列等处理。

---
2. 核心概念
2.1 vTPM 命名
「预留 vTPM」是规范命名，对内对外统一。"预留 TPM"是早期命名，没有体现转换系数问题，存在歧义，不再使用。
vTPM 是抽象资源配额单位，本质上是一份"空白资源券"。1 vTPM 对应的标称吞吐能力约为该模型最小部署资源吞吐能力的 1/10，可按份组合购买。
vTPM 不是某一具体维度（输入/输出/缓存命中）的 TPM，而是一份可按系数灵活折算的统一资源单位。
相关概念说明：
- 在描述折算机制时，可能使用"基准 TPM / 元 TPM（Meta TPM）"作为描述性概念，指未折算为具体场景实际 TPM 之前的"资源单位"——这只是概念表述，不是产品名
- 用户购买"100 vTPM"，实际消耗时按 §2.2 系数折算为各场景的实际 tokens
2.2 转换系数（多维归一化）
由于不同请求场景下消耗的实际物理资源（P 算力 / D 算力 / KV Cache 显存 / 带宽）差异极大，系统通过多维转换系数把"实际 tokens"折算回"vTPM"做扣减。
维度组合：
- 输入长度区间（如 0–128k / 128–256k / 256k+）
- 缓存命中 vs 未命中
- 输出长度区间（如 0–512 / 512–4k / 4k+）
官方示例（GLM 5.1，来自公有云业务场景 wiki，"基准 TPM"为 wiki 原文表述，指 vTPM）：
预留 100 基准 TPM（即 100 vTPM），请求输入长度 100k：
- 命中 Cache 50k → 占 1.25k TPM 额度（系数 1/40）
- 未命中 Cache 50k → 占 50k TPM 额度（系数 1:1）
- 输出 1k → 占 3k TPM 额度（系数 3:1）
从示例可见：
- 命中缓存的输入 tokens 资源消耗极低（40 个 token 折算 1 个 vTPM）
- 未命中缓存的输入 tokens 与 vTPM 1:1
- 输出 tokens 资源消耗是 vTPM 的 3 倍
- 同一请求不同部分按场景叠加不同系数
系数定义方式：
- 初始阶段参照 Serverless 定价比例设计（短期内对齐 Serverless 价格关系）
- 最终通过性能测试矩阵校准
- 系数本质是对 token 进行另一种单位的估值定价（异构资源消耗的归一化、证券化方案）
2.3 分级资源模型
vTPM 资源在物理层面预留，但存在内部分级：
层级
定义
Serverless 占用权
刚性层
已售已用部分的线性外推用量 + 冗余系数
不可侵占
可借用层
已售未用部分扣除刚性层
允许 spot 占用，vTPM 优先
未售层
资源池剩余未预留部分
允许任意占用
关键原则：vTPM 本身不超售，超额收益来自 Serverless 对已售未用层的借用。

---
3. 资源机制
3.1 外推与刚性层计算
目的：在 vTPM 不超售的前提下，允许 Serverless 借用 vTPM 用户"已购未用"的部分资源，同时为 vTPM 用户瞬时回拉留出驱逐时间窗口。
外推计算：
- 外推窗口 = 60s
- 低通滤波周期 15s × x=2，60s 内可爬坡 4 个周期
- 单账户最大爬坡倍数 = 2^(60/15) = 2^4 = 16 倍当前并发
- TPM 增长同理，线性预测
账户活跃度区分：
- 活跃账户：按当前并发 × 16 做线性外推
- 非活跃账户：不活跃但已购，每个账户有初始并发（如初始 10），需纳入预留
- 示例：10 个账户，3 个活跃（外推），7 个非活跃 × 初始 10 = 额外 70 并发
刚性层公式：
刚性层并发预留 = (Σ 活跃账户外推并发 + Σ 非活跃账户初始并发) × 冗余系数
活跃账户外推并发 = 当前并发 × 2^(60/15) = 当前并发 × 16
冗余系数：乘子（如 1.2），作用于前面计算结果之后。
击穿处理：刚性层被击穿时，请求层主动牺牲 Serverless（见 §3.3）。
3.2 动态 RL（动态限流 + 低通滤波）
Serverless 和 vTPM 都存在资源动态调度的问题，不能沿用静态值限制方式。动态 RL 由两个并列机制组成：动态限流（调整基数）+ 低通滤波（控制爬坡）。
现状 RL（将被替代）：Serverless 现状采用静态 Category 分级 Ratelimit（0–5 & 10 用量等级、按月升降级、Category 配置调整需重启网关），将被动态 RL 替代。vTPM 自起步即采用动态 RL。
3.2.1 动态限流（调整基数）
机制
说明
基数动态调整（PID）
每个模型指定 Ratelimit 初始值，根据实际 TPS 指标动态调整。请求回落/算力冗余时普遍调高；请求增长/算力缩容时普遍调低
客户宽容系数
客户实际限流值 = 基础值 × 系数。OR 场景：峰时系数 <1，谷时 >1；号池场景：识别后赋予远 <1 的系数拉高限流命中率
vTPM vs Serverless 的差异：

vTPM
Serverless
限流基数
购买量按系数折算后的占用（固定，硬承诺）
PID 动态调整
宽容系数
不适用（基数即承诺）
适用（OR 峰谷、号池识别）
vTPM 的基数由购买量决定，是硬承诺，不参与 PID 动态调整。vTPM 的限流主场景是"实际折算占用 > 购买量"。
3.2.2 低通滤波（控制爬坡）
低通滤波是动态 RL 的另一个子机制，对 vTPM 和 Serverless 均生效：当前并发限制为上一拦截周期的 1+x%。
类型
周期
x
爬到 2 倍并发
爬到 10 倍并发
vTPM
15s
2（即 1+100%，允许翻倍）
15s
~50s
Serverless
90s
1.2（即 1+20%）
~342s
~1134s
低通滤波把用户的突发请求"软化"成爬坡曲线，给 Serverless 驱逐留出节奏窗口。vTPM 的爬坡容忍度显著高于 Serverless。
3.3 救急路径
当 vTPM 实际消耗击穿刚性层时，触发救急链路。按 Serverless 是否有独立资源分两种情况：
情况 1：Serverless 有独立资源
- 仅已路由到 vTPM 资源上的 Serverless 请求需要被中断
- 这部分请求可被网关层 buffer 住调度回 Serverless 资源接续推理
- 存在 Decoding 阶段约束生成的状态控制问题（迁移可行性上限）
情况 2：Serverless 无独立资源
- 先拦截增量请求（拒新）
- 必要时主动放弃 Serverless 请求（Abort）
节点内 QoS（非救急手段）
- 请求在 P/D 节点上完成前一 batch 后暂缓进入下一 batch
- 属于预留资源内的 QoS 保障机制
- 依然占用显存、内存、带宽资源，不是救急手段
3.4 SLO 感知扩缩容
- P/D 服务独立部署
- 扩容通常激进，缩容稳定窗口较长
- 低通滤波与扩缩容之间不应存在事实矛盾
- 通过扩缩容保障 TPM、TPS、TTFT、TPOT 等指标

---
4. SLO 与赔付
4.1 SLO 承诺
维度
承诺
TPM
每分钟可消耗 N vTPM（N = 购买量）
TTFT
P95 分钟级，阈值待定
TPOT
P95 分钟级，阈值待定
时间粒度
分钟级（不承诺瞬时峰值）
承诺口径
vTPM 单位；实际 tokens 通过系数表公开可试算
关键设计：
- 系数公开透明、可实时试算
- 已售 vTPM 的系数冻结，不调整
- 系数调整仅对后续生效；确需调整已售 vTPM 的系数时，按 §5.5 SKU 转换规则处理（有利客户平台主动转换，不利客户通常不转换让利给客户，确需强制转换则补偿）
- 瞬时峰值不承诺是 Decoding 迁移约束的代价
4.2 违约事件分类
两种窗口粒度：
场景
窗口
用途
即时 SLO 监测
60s 滑动窗口
实时告警、运维响应
事后违约判定
固定分钟窗口
月度对账、赔付判定
即时监测发现问题触发告警，但不直接判定违约；事后判定用于赔付。
类别
事件
判定
A 类
vTPM 速率违约
某一固定分钟窗口内，用户实际请求消耗 vTPM ≤ 购买量但被限流/拒新
B 类
延迟违约
某一固定分钟窗口内 P95 TTFT 或 TPOT 超承诺阈值
C 类
可用性违约
某一固定分钟窗口内请求全部失败（5xx/超时）
关键区分：用户自身打满（实际需求 > 购买量）→ 限流是产品设计，不违约；用户实际需求 ≤ 购买量却被限流 → 系统侧违约。
4.3 赔付设计
基数：当月已付预留费（非按量消费费）
阶梯（按月内累计违约分钟数）：
累计违约分钟/月
赔付比例
附加权
≤ 3 分钟
0%
容忍窗口
4–30 分钟
10%
—
31–120 分钟
30%
—
> 120 分钟
100%
触发退出权，进入协商流程（见下）
退出权处理（违约 > 120 分钟）：
- 平台目标是挽留而非退费了之，优先协商解决方案
- 确需退费的，按损失填平原则，按用户实际损失赔付
- 赔付最大不超过订单金额
- 此场景不适用 §5.4 普通残值退订路径
触发与对账：
- 系统自动检测违约事件
- 每月生成违约报告并推送
- 控制台提供请求级监控面板（实际 vTPM 需求、限流事件、P95 延迟、扩缩容事件）
- 用户确认后自动发放抵扣券 / 服务费返还
- 争议仲裁依据完整请求日志（见 §4.4）
- 用户 30 天内可申诉（见 §8.6）
边界处理：
- 刚性层被击穿（救急链路失效）：算系统违约
- 维护窗口：提前 7 天通告，窗口内不计违约
- 上游故障（GPU 宕机等）：仍算违约，可用不可抗力条款封顶赔付
4.4 日志与举证
- 请求日志长期保留
- 对用户开放 90 天聚合统计数据的查询，不提供明细查询
- 聚合维度尽量全面
- 平台日志缺失是平台的问题，风险由平台承担，应采信用户申诉

---
5. 定价模型
5.1 价格基准
资源成本 + 毛利 定价，按 1/10 资源切片设计：
- vTPM 本质是算力资源切片（GPU + 内存 + 带宽 + SSD）
- 1 vTPM 单价 = 该模型最小部署资源成本 × 1/10 + 毛利
- 与 Serverless 价格的折算关系作为"计算器"功能提供给用户，辅助购买决策
- Serverless 价格不作为定价锚点
5.2 售卖规格
项目
规格
售卖时长
30 天固定
起购数量
1 vTPM
长周期售卖
暂不提供（模型迭代节奏快，新模型发布产生迁移诉求）
短期档（小时/天）
暂不提供（短期需求走 Serverless）
5.3 端点绑定模式
调用 API 同 Serverless 对话模型（统一兼容 API）。用户购买时选择 model name 绑定模式：
模式
行为
超量请求处理
适用场景
绑定公共 model name（默认）
超限请求自动降级到该模型的 Serverless 流量
按 Serverless tokens 计量计费
默认模式，平滑溢出，最常见
绑定专属 model name
配置专属 model name，用户自行控制是否使用
超量请求拒绝（429），用户自行处理限流降级
用户对用量有强控制诉求，需独占性
关键差异：
- 绑定公共 model name 时，"是否使用 vTPM"由系统自动判定（满足使用条件就会抵扣 vTPM 配额）
- 绑定专属 model name 时，用户可以自行控制是否启用 vTPM（通过指定 model name），但需要自行处理限流降级
- 专属 model name 在 vTPM 产品中对应"独立端点"语义，资源独占性更强
5.4 退订与残值
触发：用户主动退订或换购另一个模型的 vTPM 商品（走客服工单，非产品化入口）
残值计算：
残值 = 购买金额 - max(已过时长折算值, 实际使用 tokens 按 Serverless 计价的价值)
- 取 max 是关键设计，避免"用得多反而退得少"的悖论
- 暂不设计手续费
- 通过客服工单等渠道线下响应
5.5 SKU 转换规则
场景
处理
增加用户利益（如系数优化）
平台主动转换
减损用户利益
原则上不进行转换，用户也不会主动转换
平台缺失需强行转换
平台承担损失 + 补偿（vTPM 量或时长）
5.6 模型迁移的财务处理
模型升级复用 model name 时不涉及迁移；版本区分的新 model name 出现时，用户若需更换，走"残值退回 + 自行换购"流程（见 §6）。
5.7 交付流程
主流程：基础条件 → 意向/订单 → 交付 → 调用 → SLA
基础条件：用户已开通账户并完成认证（同 Serverless）。
vTPM 交付的必备能力
vTPM 因转换系数逻辑的存在，无法线下手工交付。线下只能完成服务部署和端点提供，无法做到符合预期的限流和计量。完整交付 vTPM 需要以下系统化能力配套：
能力
说明
缺失影响
服务部署 + 端点提供
模型服务部署、API 端点交付
无法调用（线下可完成）
转换系数机制
多维系数归一化折算
无法按 vTPM 配额限流、无法按场景计量
动态 RL
动态限流（PID 基数调整）+ 低通滤波
无法按购买量执行限流、无法控制爬坡
库存核验
在线购买时系统自动核验库存
无法保证订单成立即资源到位
计量计费
按 vTPM 扣减 + Serverless 溢出计量
无法对账、无法赔付
任一能力缺失则 vTPM 产品无法完整交付，只能算"服务部署 + 端点提供"，无法兑现限流计量和 SLO 承诺。
调用 vs SLA 是产品契约的两个阶段：
- 调用：用户使用 vTPM 配额发起推理请求
- SLA：平台按 §4 承诺 SLO，违约按赔付设计处理

---
6. 模型生命周期与迁移
6.1 model name 规则
- 平台制定 model name
- 平滑替代的模型升级复用已有 model name（对用户透明）
- 有明确版本区分的遵从版本号（新 model name）
6.2 合约期内模型更换
- 合约期内原则上不允许更换 model name
- 确需更换 → 残值退回 + 用户自行换购另一个模型的 vTPM
- 不提供功能性入口，走客服工单
6.3 模型停售与停服
- 模型保留期 ≥ 已售预留 vTPM 的最长到期时间
- 尾部资源由 Serverless 请求填充
- 确需提前停止服务时：属于 §5.5 SKU 转换规则中的"平台缺失需强行转换"场景，平台承担损失 + 补偿（vTPM 量或时长）
6.4 SKU 售卖窗口
- SKU 售卖窗口在后台配置
- 通常仅在预计停售前配置停售日期
- 停售 ≠ 已存续 vTPM 合约中断
- 停售仅指：
  - 不再售卖新订单
  - 不再接受到期日在停售日期之后的续约

---
7. 库存与销售运营
7.1 库存口径
- 库存 = 销售当时剩余资源按当前模型吞吐能力计算的可交付数量
- A 模型的 vTPM 售出后，对应资源视为已预留，不再参与后续库存计算（按模型强隔离）
- 未预留资源是池化的，可被任意模型占用
- 预留后仅允许一定水位线以下的 spot 占用
- vTPM 本身不超售，库存数字精确；销售系统与资源系统出现瞬时偏差时，按先到先得 + 失败方候补处理
7.2 预下单锁位
- 类似电影票预先锁定座位机制
- 销售前可验证库存可用性
- 对内通过数据看板观察
7.3 续约机制
- 有效预留 vTPM 存续期内 + SKU 售卖窗口期内，用户可任意续约
- 自动续约：下单时允许用户勾选
- 续约通知节奏：T-7、T-3、T-1
- 到期日超过停售日期的续约不被接受
7.4 订单校验
- 订单流程中必须有资源锁定校验
- 校验不成功不能允许订单成立
7.5 库存可见性
- 实时显示库存数字
- 售罄提供候补 / 替代模型推荐
- 销售人员可为企业客户锁库存（限额）

---
8. 用户触点与运营闭环
8.1 用户生命周期触点
阶段
用户动作
平台响应
关键信息
购前
查看库存/试算
计算器 + 实时库存
模型可选量、等效 QPS、价格
下单
选模型/数量/端点
资源锁校验 → 订单成立
合约起止、系数表、SLO 条款
使用中
发请求
路由 + 调度 + 限流
实时用量、剩余配额、限流计数
观察
查看监控
指标面板
vTPM 消耗、实际 tokens、P95 延迟、SLO 命中
救急
被限流/违约
告警 + 报告
限流事件、违约判定、补偿
续约/退订
续约/退订/换购
SKU 校验 + 残值计算
续约价、残值明细
模型迁移
主动/被动迁移
客服工单 + 补偿规则
迁移影响、补偿方案
8.2 监控指标（用户可见，90 天聚合）
- vTPM 实际消耗 vs 购买量（按分钟）
- 限流事件（原因码：用户超限，即实际折算占用 > 购买量）
- P95 TTFT、P95 TPOT（按分钟，含分母请求量）
- 系数表与实际扣减明细
- SLO 命中率与违约事件
8.3 告警与通知
事件
通知方式
时效
到期 T-7 / T-3 / T-1
站内信 + 邮件 + SMS
阶梯触发
续约窗口开放
站内信
T-7
库存告罄/补货
站内信 + 邮件
实时
限流率异常
告警
分钟级
SLO 违约
邮件 + 工单
事件触发
模型停售/迁移
邮件 + 控制台强提示
事件触发
8.4 控制台模块
- 预留管理：vTPM 列表（模型、数量、起止、端点、状态）、续约、退订、换购入口
- 用量监控：实时用量曲线、限流事件、SLO 命中率、P95 延迟曲线
- 成本分析：vTPM 费用 + 溢出 Serverless 费用 + 等效按量对比（差异化竞争力）
- 库存与购买：库存看板、预下单、计算器、购买入口
- 事件中心：限流事件、违约事件、通知记录、工单
- 配置：端点绑定、报警阈值、告警订阅
8.5 试用与代金券
- 不普遍提供免费试用（vTPM 库存稀缺）
- 不普遍提供无理由退订机制
- 销售可申请必要的代金券抵扣相应费用
- 不设计普遍机制
8.6 违约判定与申诉
- 系统自动判定 + 推送
- 用户 30 天内可申诉
- 仲裁依据：请求级日志
- 平台日志缺失 → 默认采信用户申诉（倒逼日志基建完整）
8.7 内部运营指标
- vTPM 库存周转率（卖空速度 vs 补容速度）
- 续约率（按月）
- SLO 违约率（按月）
- 资源利用率（vTPM 刚性层 + Serverless 借用层）
- 救急链路触发频次（abort 请求量、迁移请求量）
- 模型迭代节奏 vs vTPM 合约到期节奏的匹配度

---
9. 关键 Q&A 汇总
Q1：vTPM 预留的本质是什么？硬预留还是配额预留？
A：物理层面要做资源预留，但同时进行分级占用：
- 已售已用部分及线性外推用量 → 刚性预留，保障交付
- 已售未用部分（扣除外推） → 允许 Serverless 占用
- 未售部分 → 允许 Serverless 占用
vTPM 本身不超售，超额收益来自 Serverless 对已售未用层的借用。vTPM 商品本质是算力资源切片（GPU + 内存 + 带宽 + SSD）。
Q2：1 vTPM 的标称吞吐如何定义？
A：初始阶段直接参照 Serverless 的定价设计比例；本质上，区分各维度设计转换系数是一种针对不同场景的归一化、证券化方案；最终靠性能测试矩阵得到。
系数定义后，已售 vTPM 的系数冻结，调整只对后续生效，确需调整走 SKU 转换逻辑。
Q3：外推窗口和方法如何定义？
A：动态 RL = 动态限流（PID 调整基数 + 客户宽容系数）+ 低通滤波（陡增拦截）。vTPM 低通滤波周期 15s、x=2，Serverless 周期 90s、x=1.2。vTPM 的限流基数是购买量按系数折算后的占用（固定，硬承诺），不参与 PID 动态调整；Serverless 的限流基数由 PID 动态调整。
外推窗口和方法对照低通滤波的爬坡曲线，并按一个比例系数做冗余。若外推和冗余均被击穿，只能在请求层牺牲 Serverless（服务端主动中断）。
SLO 感知和流量分发必须是亚分钟级。
Q4：Serverless 中断的具体语义是什么？
A：分两种情况：
- 情况 1（Serverless 有独立资源）：仅已路由到 vTPM 资源上的请求需要被中断；这部分请求可被网关层 buffer 住调度回 Serverless 资源接续推理，但存在 Decoding 阶段约束生成的状态控制问题。
- 情况 2（Serverless 无独立资源）：先拦截增量请求，必要时主动放弃 Serverless 请求（Abort）。
节点内 QoS（完成前一 batch 后暂缓进入下一 batch）是预留资源内的 QoS 保障机制，重要但不是救急手段（依然占用显存、内存、带宽）。
救急链路只影响 Serverless 挽救率，不影响 vTPM SLO。
Q5：vTPM 用户的 SLO 承诺形式？
A：
- TPM：承诺"每分钟可消耗 N vTPM"，系数公开透明、可实时试算
- 延迟：P95 分钟级承诺
- 时间粒度：分钟级（不承诺瞬时峰值，作为 Decoding 迁移约束的代价）
- 系数口径：已售 vTPM 系数冻结，调整走 SKU 转换
- Serverless：无 SLA，仅提供指标参考
Q6：违约/赔付如何设计？
A：
- 违约分 A/B/C 三类（速率违约、延迟违约、可用性违约）
- 区分"用户自身打满"与"系统侧违约"
- 赔付基数为当月已付预留费
- 阶梯赔付：≤3 分钟容忍 → 4–30 分钟 10% → 31–120 分钟 30% → >120 分钟 100% + 触发退出权进入协商流程（挽留优先，确需退费按损失填平原则，最大不超过订单金额）
- 用户 30 天内可申诉，平台日志缺失时采信用户申诉
Q7：定价如何锚定？
A：资源成本 + 毛利定价，按 1/10 资源切片设计。与 Serverless 价格的折算关系作为"计算器"功能提供给用户辅助决策，不成为定价锚点。暂定 30 天固定规格，不提供长周期售卖。
Q8：超限请求如何处理？
A：两种端点绑定模式（见 §5.3）：
- 绑定公共 model name（默认）：超限请求自动降级到该模型 Serverless 流量，按 Serverless tokens 计量计费
- 绑定专属 model name：超量请求被拒绝（429），用户自行处理限流降级
Q9：合约期内模型更换怎么处理？
A：合约期内原则上不允许更换 model name。确需更换时，走"残值退回 + 自行换购"：
残值 = 购买金额 - max(已过时长折算值, 实际使用 tokens 按 Serverless 计价的价值)
通过客服工单线下响应，不提供功能性入口。
Q10：模型停售与停服的区别？
A：
- 停售：不再售卖新订单，不再接受到期日在停售日期之后的续约；已存续 vTPM 合约不中断
- 停服：提前停止服务，平台承担迁移成本 + 补偿预留量或时长
- 模型保留期通常超过已售预留 vTPM 的最长到期时间，尾部资源由 Serverless 请求填充
Q11：库存口径如何定义？
A：
- 库存 = 销售当时剩余资源按当前模型吞吐能力计算的可交付数量
- A 模型 vTPM 售出后对应资源钉死，不再参与后续库存计算（按模型强隔离）
- 未预留资源池化，可被任意模型占用
- 预留后仅允许水位线以下的 spot 占用
- 订单流程必须有资源锁定校验，校验失败订单不成立
- vTPM 本身不超售，库存精确，不允许软超售
Q12：续约机制怎么设计？
A：
- 在有效预留 vTPM 存续期 + SKU 售卖窗口期内，用户可任意续约
- 下单时可勾选自动续约
- 续约通知节奏：T-7、T-3、T-1
- 到期日超过停售日期的续约不被接受
Q13：日志与举证如何设计？
A：
- 请求日志长期保留
- 对用户开放 90 天聚合统计数据查询，不提供明细查询，聚合维度尽量全面
- 平台日志缺失是平台的问题，风险由平台承担，应采信用户申诉
Q14：是否提供免费试用 / 无理由退订？
A：不普遍提供。销售可以申请必要的代金券抵扣相应费用，但不设计普遍机制。
Q15：SKU 转换规则？
A：
- 增加用户利益 → 平台主动转换
- 减损用户利益 → 原则上不进行转换，用户也不会主动转换
- 平台缺失需要强行转换 → 承担损失 + 补偿（vTPM 量或时长）
Q16：vTPM 在公有云产品矩阵中的位置？
A：vTPM 是 MaaS 层标准产品，与 Serverless API 同层。专属实例介于 MaaS 与部署层之间，FaaS 是底层部署形态，Tokens 包是支付手段，均不在本文档范围。
- vs Serverless：Serverless 共享无保障，vTPM 是切片硬预留
- vTPM 与专属实例的核心区别：专属实例是独占资源 + 定制 SLA，vTPM 是标准化切片 + 标准 SLO
- 折算机制是平台级能力：vTokens 包（Tokens 包演进形态）会独立应用，系数与 vTPM 不严格一致
Q17：vTPM 与各场景实际 TPM 如何转换？真实示例？
A：以 GLM 5.1 预留 100 vTPM 为例，请求输入长度 100k：
- 命中 Cache 50k → 占 1.25k TPM 额度（系数 1/40，40 个 token 折算 1 vTPM）
- 未命中 Cache 50k → 占 50k TPM 额度（系数 1:1）
- 输出 1k → 占 3k TPM 额度（系数 3:1）
同一请求不同部分按场景叠加不同系数。短期内转换系数对齐 Serverless 价格关系，最终靠性能测试矩阵校准。
Q18：vTPM 与 Serverless 的爬坡能力差异？
A：低通滤波是动态 RL 的子机制之一（另一子机制是动态限流），对 vTPM 和 Serverless 均生效，参数差异化：
类型
周期
x
爬到 2 倍并发
爬到 10 倍并发
vTPM
15s
2
15s
~50s
Serverless
90s
1.2
~342s
~1134s
vTPM 用户拥有更高的瞬时爬坡容忍度；Serverless 几乎不能接受陡增。真正的短时陡增场景两种产品都不适合，需在客户端做请求平滑、buffer、异步队列等处理。
Q19：绑定公共 vs 专属 model name 的差异？
A：
- 绑定公共 model name（默认）：超限请求自动降级到该模型 Serverless 流量，按 Serverless tokens 计费；"是否使用 vTPM"由系统自动判定
- 绑定专属 model name：用户配置专属 model name，可自行控制是否启用 vTPM；超量请求被拒绝（429），用户自行处理限流降级；资源独占性更强
Q20：交付流程的前提条件？
A：vTPM 因转换系数逻辑的存在，无法线下手工交付。线下只能完成服务部署和端点提供，无法做到符合预期的限流和计量。完整交付 vTPM 需要系统化能力配套：转换系数机制、动态 RL、库存核验、计量计费。任一能力缺失则 vTPM 产品无法完整交付，只能算"服务部署 + 端点提供"，无法兑现限流计量和 SLO 承诺。
当前文档处于需求澄清阶段，尚未开始实施，描述的是目标形态。

---
10. 待后续讨论的开放问题
以下问题在本次讨论中未充分展开，留待后续：
1. 具体 SLO 阈值：P95 TTFT、TPOT 的具体数值待性能测试矩阵确定
2. 冗余系数的具体取值：外推窗口已定（60s），爬坡倍数已定（16），冗余系数为乘子，具体数值待基于实际流量模式校准
3. 水位线数值：spot 占用水位线的具体阈值
4. 库存补容触发阈值：低于多少触发补容订单
5. 企业客户锁库存限额：销售可锁定的库存上限
6. 代金券审批流程：销售申请代金券的规则与额度
7. 不可抗力条款封顶：上游故障时的赔付封顶机制
8. 多区域/多集群调度：跨区域资源调度与成本摊销
9. P/D 分离架构下的具体调度策略：P 节点与 D 节点独立扩缩容与 vTPM 配额的映射
10. 系数表的初始版本：基于 Serverless 定价比例的具体初始系数表